# MochiPort v0.5.10

本版本收敛概览页的展示：移除底部的账号池摘要行，请求速度单位由「每分钟」改为「每秒」；同时清理了消息渠道的一批死代码。

## 概览页

- 移除底部的 Sub2API 账号池摘要行。侧栏的「账号池」页面不受影响，仍可查看完整账号与余额明细。
- 「当前请求 Token」的速度单位由每分钟改为每秒，菜单栏的「消耗速度」条目同步调整。底层统计仍按分钟汇总，仅在显示时换算。
- 速度数值保留一位小数：每秒速率通常只有个位数，此前的取整逻辑会把它显示成 0。

## 内部清理

- 删除消息渠道中被新实现取代的旧代码：Telegram 的旧版模型列表渲染、无调用的适配层方法与多余包装函数，共约 190 行。
- 移除 Telegram 模块整体的 dead_code 豁免，改为按性质分别标注（协议字段、测试专用），此后该模块新增的死代码会被编译器直接报出。

## 验证

- Rust 全量测试通过（1116 项），macOS Swift 测试通过（201 项）。
- macOS 正式 App 经受保护交接完成 daemon 版本切换，切换后健康检查与 IM 渠道重连正常。
- 概览页截图复核：账号池摘要行已移除，速度显示为「当前请求 Token/秒」。
