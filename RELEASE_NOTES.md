# MochiPort v0.5.7

本版本为 Telegram 带来可随时切换的回复颗粒度三档，并把 Codex 接入页重构为完成即打勾的引导清单；同时更新 AI 网关模型目录并优化管理表格的可读性。

## 核心改动

- 新增 Telegram 回复颗粒度三档，设置即时生效、无需重启：
  - 摘要回复：只发过程文本（Codex 的说明文字）和最终结果，工具执行、文件修改、推理、计划、搜索一律静默；
  - 标准回复（默认）：现有行为，所有信息合并进一条聚合气泡原地更新；
  - 完整回复：所有信息逐条独立发送，每条命令执行、文件修改、推理、计划各自一条消息，不合并更新。
- 错误通知与审批卡片不受颗粒度影响，任何档位都照常送达。

## 消息渠道界面

- Telegram 账号详情新增回复颗粒度参考卡：三档各带 Telegram 消息效果预览，点击卡片即切换。
- Telegram 内可用 `/回复`（别名 `/granularity`）切换：无参数查看当前档位，支持中英文档位别名；命令已注册进 bot 菜单与 `/help`。

## Codex 接入

- 页面重构为接入引导清单：连接、模型服务、配置、登录、桌面控制、远程控制六步，完成自动打勾，当前步骤给出操作入口，异常步骤给出原因与“自动修复”。
- 全部步骤通过后展示“全部就绪”横幅与启动入口；“启动前会做什么”说明收进横幅。
- 修复完整颗粒度下过程文本被折叠进「较早进展」的问题：现在过程文本与其余事件一样逐条独立发送。

## AI 网关与其他

- 模型目录新增 GPT-6-Astra（xhigh 多代理推理强度与最新官方指令模板），GPT-5.6 系列顺延排位。
- 会话与请求日志列表增加斑马纹底带，提升多行数据扫读性。

## 验证

- Rust 全量测试通过（1078 项）。
- macOS Swift 测试通过。
- macOS 正式 App 经受保护交接完成多轮 daemon 版本切换（build 500 → 503），切换后 Telegram 与飞书均自动重连正常。
