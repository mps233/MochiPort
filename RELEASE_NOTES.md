# MochiPort v0.5.9

本版本以稳定性与资源占用为主：修复了后台服务日志无限增长、远程控制连接风暴与排空卡死三个问题，并把概览页的常驻 CPU 占用降到接近零；同时修正了通知开关的两处漏接线。

## 后台服务

- 日志不再无限增长。此前 daemon 的 stdout 被 launchd 捕获到 `mochiport-daemon-launchd.log`，而日志上限只作用于另一个文件，实测 13 天涨到 1.57 GB。现在该文件在启动时和运行期都受 `logging.maxMb` 约束。
- 修复远程控制连接风暴。服务排空期间原本先完成 WebSocket 升级再断开，客户端零退避重试，曾在一个小时内产生 33 万次连接。现在排空时在升级前直接拒绝并返回 `Retry-After`，拒绝日志同时做了锁存。
- 修复排空卡死。排空等待加入 300 秒上限，超时后取消排空并恢复服务，而不是让 daemon 永久处于 draining 状态（此前曾导致远程控制 40 分钟不可用）。

## 客户端

- 概览页常驻 CPU 从约 34% 降到接近零。用量趋势聚合改为按数据修订号缓存，避免每次界面重排都查询一次 SQLite。
- 概览吉祥物动画改为可选开关，默认关闭。动画会让界面按显示器刷新率重排，窗口打开时约多占 11% CPU；需要时可在设置中打开。
- 通知开关修复两处漏接线：同一轮评估产生的多个事件此前只投递第一个，其余被静默丢弃；「检测新版本」此前只控制横幅，关闭后仍会请求 GitHub，现在会真正拦下检查。

## 验证

- Rust 全量测试通过（1116 项），macOS Swift 测试通过（201 项）。
- macOS 正式 App 经受保护交接完成 daemon 版本切换，切换后健康检查与 IM 渠道重连正常。
- 日志上限实测：启动时把 1.57 GB 的捕获文件截断到 171 字节且不产生空洞；运行期把 8.6 MB 压回预算内。
- 概览页 CPU 实测（菜单栏窗口打开）：优化前约 34%，优化后动画关闭时约 0%，打开动画时约 11%。
