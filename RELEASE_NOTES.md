# MochiPort v0.5.11

本版本给 AI 网关补上内置模型库与请求日志保留策略，并把 Telegram 的标准回复收敛成一条聚合气泡。

## 模型目录

- **内置模型库**：仓库内嵌一张模型能力表（`model_library.json`，3700+ 个模型，来源 models.dev）。只要模型名出现在**已启用服务商的模型列表**里，目录就自动填好显示名、上下文窗口、图片输入等能力，不必手工声明；自定义条目优先级更高。
- **协议家族模板**：各协议家族（OpenAI Responses / DeepSeek Responses / Grok Responses / Chat Completions / Anthropic Messages）的 Codex 私有协议字段抽到 `family_templates.json`。模型目录里因此只保留真实在用的模型，不再出现「只当模板用」的空条目。
- **官方目录跟随**：启动时与每 24 小时从 openai/codex 同步一次官方模型目录到用户级覆盖文件，支持跟随官方下线。
- **运行时更新模型库**：数据目录里放一份 `model_library.json` 即可覆盖内置库；`POST /api/v1/manage/gateway/model-library/reload` 或 GUI「使用记录」区的「重新加载模型库」按钮可免重启生效。文件损坏或缺少非空 `models` 时保留内置库。
- 修正数据目录推导：原先用 `config.toml` 的父目录，在 `MOCHIPORT_HOME` 与配置文件位置不一致时会指向错误目录，导致官方目录覆盖与模型库覆盖都读不到。

## 请求日志

- 新增保留策略：默认保留 **30 天**、总量上限 **256 MB**，daemon 启动时及每小时清理并 VACUUM 回收空间。实测把本机库从 37.4 MB 降到 17.3 MB。
- 两个维度设为 0 表示完全手动管理；GUI「网关 → 概览 → 使用记录」可调整或手动清空。

## Telegram 单气泡过程卡

标准颗粒度下，一个 turn 只保留**一条**聚合气泡：

- 过程文案（Codex 的说明文字）直接可见，工具/命令步骤收进折叠的 `工具摘要（N）`，子代理活动并入同一条气泡的「协作」折叠块。
- 过程文案与工具步骤按**到达序号交错**渲染，不再各自并成一大批。此前思考的序号只在 `item/completed` 才分配、而工具在 `item/started` 就分配，导致思考永远排在工具之后、被并进上一个工具摘要。
- 思考摘要折成**一行**（超出截断并补 `…`），用等宽蓝底 `code` 呈现，与底部 `turn <id>` 同一种样式。
- **最终回复内嵌**在同一条气泡里，做成默认展开的 `最终回复` 折叠块，不再单独发 `✅ 已完成` 气泡；turn 耗时拼到卡头（`执行完成 · 2 步 · 25秒`）。
- 最终回复里的 markdown（表格、标题、列表、链接、斜体、围栏代码）都会渲染成对应块，不再露出 `|---|`、`##`、`[文字](路径)` 这类原文。

## 修复

- **修复 Telegram 死锁**：`turn/completed` 曾把 `runtime.lock()` 写在 `if let` 的条件里，而 `if let` 的临时值会存活到整个语句结束（包括函数体），函数体内再次申请同一把锁即构成自我死锁——daemon 会永久卡死该请求，并连带拖垮管理接口 `/api/v1/manage/lifecycle`、租约续期和版本交接。锁已改为独立语句。
- 修复显式 `phase = final_answer` 的答复被重复发送（既单独发卡片、又内嵌进气泡）。
- 修复最终回复内的本地图片在改为内嵌后静默丢失。
- 修复版本切换被拖慢的问题：升级协调原先只在常规刷新周期内运行，实测干等 169 秒；现在待切换期间改用 3 秒间隔，实测降到 41 秒。

## 验证

- Rust 全量测试通过（1178 项），macOS Swift 测试通过（201 项）。
- macOS 正式 App 经受保护交接完成 daemon 版本切换，切换后健康检查、管理租约与 IM 渠道重连正常。
- Telegram 实机复核：标准回复为单条聚合气泡，思考摘要为一行蓝色等宽样式，最终回复内嵌且默认展开，表格与列表正常渲染。
