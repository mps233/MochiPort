# MochiPort v0.5.8

本版本重点升级账号池页面：新增 Sub2API 分组列，余额改为站点级显示并按钱包去重，"同一账号挂多个渠道"不再重复累加；同时把消息渠道的审批与错误文案本地化成人话，并简化了 AI 网关服务商编辑器。

## 账号池

- 新增 Sub2API 分组列，位于状态列之前；每个分组渲染为胶囊标签，一排放不下自动折行，全部可见。
- 多账号站点的折叠行精简为纯标题行：只保留站点名、账号计数和展开箭头，分组、状态、调度明细统一在展开的子账号行里查看。
- 余额改为站点级显示：钱包余额在站点行显示一次；与兄弟渠道共享同一钱包的子行余额留空，不再同一钱包显示多遍。
- 余额合计按钱包去重：daemon 探测时按（站点, 密钥）标记共享钱包，同密钥、或同站点下余额完全一致的渠道视为同一钱包，合计只计一次——中转站"一个账号开多把 key"的常见形态同样生效。
- 分组名解析兼容各版本 Sub2API：账号列表自带的分组名优先，缺失时用管理端分组目录补齐，再缺失回退"分组 <id>"。
- 站点行悬停可查看该站点每个渠道的余额明细。

## 消息渠道

- 审批卡片按语言本地化审批类型与摘要，任务失败错误映射为一句人话提示并保留原始原因。
- 审批摘要隐藏协议噪音：unknown 动作类型与 execpolicy 修正条目不再展示。
- 客户端管理操作错误统一映射为人话文案。

## AI 网关

- 服务商编辑器简化：协议、兼容配置、模型映射等低频字段收进"高级设置"折叠区，主表单只留服务商、地址与密钥。
- 选择常用服务商模板自动填好地址与协议，选"自定义"时自动展开高级设置；常用字段补充悬停帮助提示。

## 验证

- Rust 全量测试通过（1109 项），macOS Swift 测试通过（199 项）。
- macOS 正式 App 经受保护交接完成 daemon 版本切换（build 512 → 513），切换后 Telegram 与飞书自动重连正常。
- 余额去重在真实账号池实测：合计由重复累加的 $82.36 修正为 $64.81，共享钱包子行不再重复显示余额。
